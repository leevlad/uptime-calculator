- title "Incidents"

%table.table.table-striped
  %thead
    %tr
      %th Id
      %th Incident Time
      %th Summary
      %th Faulted service
      %th Contact people
      %th Tags
      %th Hosts
      %th Task url
      %th
      %th
      %th

  %tbody
    - @incidents.each do |incident|
      %tr
        %td= incident.id
        %td= incident.incident_time.to_s(:short)
        %td= incident.summary
        %td= incident.faulted_service.name
        %td= incident.contact_people
        %td= incident.tags
        %td= incident.hosts
        %td= incident.task_url
        %td= button_to 'Show', incident, class: "btn btn-sm btn-info", method: "get"
        %td= button_to 'Edit', edit_incident_path(incident), class: "btn btn-sm btn-primary", method: "get"
        %td= button_to 'Destroy', incident, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: "btn btn-sm btn-danger", method: "get"

%br

